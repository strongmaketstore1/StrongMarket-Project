export default function App() {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        flexDirection: "column",
        fontFamily: "Arial, sans-serif",
      }}
    >
      <h1>StrongMarket is working ✅</h1>
      <p>GitHub Pages deployment is working correctly.</p>
    </div>
  );
}
